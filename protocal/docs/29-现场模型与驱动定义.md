# 29 · 现场交付包的模型与驱动定义（`cfg` + 插件符号）

> 用途：回答"读 `app1/`（现场 iNC-BOX-200）能不能把缺的驱动逆出来"。
>
> **结论：数据面已经齐了。** `cfg/models/<品牌>.json`（数据项）、
> `cfg/driver_defs/<品牌>_driver.json`（连接与分派）、`lua/lua_mod/*.lua`
> （模型项 → 端点 + 规整）、`lib*.so` 的 `.dynsym`（插件方法签名与项键）
> 四份叠起来，就是现场每个驱动的**定义**——逐项、逐方法、带端口和参数。
> 剩下只有一样：自研实现的**线协议字节/URL**（GSK / KEDE / Mitsubishi-HTTP 这类
> 插件自己发 HTTP、不走供应商 SDK 的），仍需一次抓包；而这一次抓包也能在本地做
> （qemu 跑 ARM 插件 + 假机器，见 §6），不必等现场。
>
> 证据分级：🟢 现场二进制/配置实测。

---

## 1 链路：谁定义什么

```
cfg/models/<品牌>.json          NC-Link 设备模型（数据项 + 采样通道）
cfg/driver_defs/<品牌>_driver.json  驱动定义（模块、IP/端口/超时、路径分派、后台任务）
lua/lua_mod/<品牌>_mod.lua      模型项 → 网关端点 + 数据规整
app1/hp2x/hp2x_box200           Go/GoFrame 设备协议网关（:33123）
app1/nclink-service + lib*.so   插件：厂商协议实现（C++，ELF32 ARM）
libfwlib32.so / libHsc3Api.so / libCommApi.so  供应商 SDK
```

| 现场文件 | 里面是什么 | 我们这边的对应物 |
|---|---|---|
| `cfg/models/<品牌>.json` | `dataItems[].type`（`STATUS`/`FEED_SPEED`/`PART_COUNT`/`POSITION`/`SPEED`/`CURRENT`/`LOAD`/`PROGRAM`/`LINE_NUMBER`/`WARNING`/`VARIABLE@…`）、`components[].configs[]`（`TOOL_PARAM`/`COORDINATE`/`FILE`/`CONSOLE`）、`SAMPLE_CHANNEL` | `ncl_adapter` 的模型（`model.json` 或按点位表生成） |
| `cfg/driver_defs/<品牌>_driver.json` | `connections[]`（`module` + `ipAddress`/`port`/`timeout`）、`drivers`（`/`、`/CONTROLLER/FILE` → client）、`tasks[]`（Lua 脚本 + 周期） | `conf/driver/*.json`：链路 + 点位表 |
| `lua/lua_mod/<品牌>_mod.lua` | `["get_value@/STATUS"] = {url=…, data_process=…}`：模型项 → 网关端点，外加规整（取整、数组标记、共享状态） | 点位表 + 规整规则（如 KND 那条 ×100 / `100%02d`） |
| `lib<品牌>.so` | 插件本体：`BaseNCMod` 派生类，**strong 覆盖**它支持的项；项键（`/GSK/CNC/STATUS` 之类）是字符串字面量 | 我们的 driver（`ncl_driver_ops`） |
| `libfwlib32.so` / `libHsc3Api.so` / `libCommApi.so` | 供应商 SDK（FANUC FOCAS、华数 Hsc3、华数的 asio 传输层） | 无——这类协议可以直接调 SDK |

---

## 2 提取方法（可复现）

`.so` 全部是 **ELF32 ARM，`.symtab` 与 `.debug_*` 都被 strip 掉了**，但
**`.dynsym` 还在**：类名、方法名、参数类型（C++ 修饰名）一个字没少。

```sh
docker run --rm -v <现场包>:/svc:ro -v $PWD:/out gcc:13 sh -c '
  for f in /svc/lib*.so; do
    b=$(basename $f)
    # 该插件真正实现的方法（strong）：T = 本插件定义，W = 继承 BaseNCMod 的默认实现
    nm -D --defined-only $f | c++filt | awk "\$2==\"T\"" | sort -u > /out/$b.methods
    nm -D -u $f | c++filt | awk "{print \$2}" | sort -u > /out/$b.imports   # 依赖的 SDK
    strings -n 6 $f > /out/$b.strings                                       # 项键与错误文本
  done'
```

三条判读规则：

1. **`T`（strong）= 插件支持的项**；`W`（weak）= 从 `BaseNCMod`（`libbase.so`）
   继承的默认实现，即"声明了但这家不支持"。所以"支持面"= strong 集合。
2. **`imports` 里的厂商函数**说明这家是"调 SDK"还是"自己实现"：
   `libfocas.so` 导入 29 个 `cnc_*`（FOCAS SDK），其余插件只导入通用库。
3. **`strings` 里的 `/品牌/…` 字面量**就是该插件认的项键，跟 Lua 里的 `url` 一一对上。

---

## 3 端口 ↔ 品牌 ↔ 模块 🟢

来源：`rmc/config.yaml` 的 `nmap.ports` / `port_model_map` / `port_driver_map`
与 `cfg/driver_defs/*.json`。**这张表让盒子能靠一次端口扫描认出发电机床的品牌**，
也给了我们每个品牌"该连哪个端口"的答案。

| 端口 | 品牌 | `module` | 模型文件 | 备注（`rmc/config.yaml` 原注） |
|---|---|---|---|---|
| 89 | 精雕 JINGDIAO | `lua`（Go 侧实现） | `jingdiao.json` | JINGDIAO CNC |
| 102 | 西门子 S7 / 840D NCU | `s7` / `lua` + `s7` | `s7.json`、`s7_cnc.json`、`s7_ncu.json` | S7 SIEMENS PLC |
| 502 | Modbus TCP / 埃夫特 | `modbus` / `lua` | `modbus.json`、`efort.json` | Modbus TCP |
| 683 | 三菱 CNC M70/M80 | `mitsubishi-http` | `mitsubishi.json` | Mitsubishi CNC M70/M80 |
| 1433 | SQL Server（金虹?） | `lua` | `sqlserver.json` | 数据库直连 |
| 5534 | 三菱 PLC MC | `mc` | `mc.json` | Mitsubishi PLC |
| 5566–5572 | 新代 SYNTEC | `lua` | `syntec.json` | SYNTEC CNC |
| 6000 | 广州数控 GSK | `gsk-http` | `gsk.json` | GSK CNC |
| 7878 | DMG MORI（MTConnect） | `lua` | `dmg_mori_mtconnect.json` | HTTP |
| 8000 | 相机 | `camera` | `camera.json` | 视觉 |
| 8193（8192–8293） | FANUC | `focas` | `focas.json` | FANUC CNC |
| 9600 | 欧姆龙 FINS | `omron-fins` | `fins.json` | — |
| 10000 | 兄弟 Brother | `lua` | `brother_model.json` | — |
| 19000 | 海德汉 LSV2 | `lua` | `lsv2.json` | LSV2 HEIDENHAIN CNC |
| 23234 | 华数 HSR3 | `hsr3`（`+Hsc3Api`） | `hsr3.json` | — |
| 4840 | 沈阳 i5 / 840D sl | `i5-opcua` / `sinumerik-arm` | `i5_opcua.json`、`sinumerik.json` | OPC UA |
| 62937 | 科德 KEDE | `kede-http` | `kede.json` | KEDE CNC |

`cfg/driver_defs/` 里还有两家没进这张表：`kuka_driver.json`（`lua`，列了 5534）、
`nachi_driver.json`（`lua`，列了 5534）——那两条端口与三菱 PLC 撞车，像是现场配置抄错，
按 KUKA/NACHI 各自的默认口再核一遍。

---

## 4 逐家：拿到了什么

“模型项”= `cfg/models/*.json` 里的数据项/配置项条数；“实现方法”= 插件 `.dynsym` 的
strong 集合（去掉模板与第三方库）。

| 品牌 | 模型项 | 插件（类） | 实现方法 | 供应商 SDK | 数据面是否够写驱动 |
|---|---|---|---|---|---|
| FANUC FOCAS | 79 | `libfocas.so`（`Focas`） | 49 | 🟢 `libfwlib32.so`：29 个 `cnc_*` | ✅ 够（直接调 SDK） |
| 西门子 840D | 79 | `libsinumerik-arm.so`（`SINUMERIKARM`） | 33 | OPC UA（节点路径是字面量） | ✅ 够（OPC UA 标准节点） |
| 西门子 840D（readvar 变体） | 77 | `libsinumerik-arm-readvar.so` | 33 | 840D `ReadVar` 方法 | ✅ 够 |
| 沈阳 i5 | 100 | `libi5-opcua.so`（`I5OpcUa`） | 45 | open62541 + mbedtls | ✅ 够（OPC UA） |
| 华数 HSR3 | 101 | `libhsr3.so`（`HsNcLink`/`HSRobot`）+ `libHsc3Api.so` + `libCommApi.so` | 67 + 265 + 304 | 华数自带 SDK | ✅ 够（API 面完整） |
| 广州数控 GSK | 16 | `libgsk-http.so`（`GSKHTTP`） | 22 | 无（自研 HTTP） | 🟡 项键齐，线协议待抓 |
| 科德 KEDE（GNC62） | 78 | `libkede-http.so`（`KEDEHTTP`） | 23 | 无（自研 HTTP） | 🟡 同上 |
| 三菱 M70/M80 | 29 | `libmitsubishi-http.so`（`MITSUBISHIHTTP`） | 28 | 无（自研 HTTP） | 🟡 同上（文件类方法齐） |
| 三菱 PLC MC | 28 | `libmc.so`（`MC`） | 13 | 无 | ✅ 够（我们已实现） |
| 西门子 S7 | 7 | `libs7.so`（`S7`） | 13 | 无 | ✅ 够（我们已实现） |
| Modbus | 5 | `libmodbus.so`（`Modbus`） | 21 | 无 | ✅ 够（我们已实现） |
| 欧姆龙 FINS | 9 | `libomron-fins.so`（`OMRONFINS`）+ `libfins.so`（C 编解码） | 13 + 18 | 无 | ✅ 够（我们已实现） |
| 相机 | 3 | `libcamera.so`（`CAMERA` + `SocketLib`） | 28 | 私有 socket | 🟡 需抓包 |
| 共享文件 | — | `libsharedfs.so`（`SharedFSMod`） | 13 | 本地文件系统 | ✅ 够（= 我们 FTP/文件通道的替代品） |

Go 侧（`module: lua`）的品牌，协议实现在 `hp2x_box200` 里，模型项与规整在 Lua
模块中，**不需要抓包就能照抄**：兄弟（52 项）、精雕（14）、海德汉 LSV2（48）、
新代 SYNTEC（14）、DMG MORI MTConnect（126）、KUKA（69）、那智 NACHI（59）、
埃夫特（50）、S7 NCU（42）、S7 CNC（13）、SQL Server（160）、凯恩帝 KND（75）、
FSK 胶机/伺服（16/24）、IO（7）、函数块（10）。

### 4.1 FOCAS：为什么"不用逆协议" 🟢

`libfocas.so` 导入的 29 个 `cnc_*` 就是这家用到的 FOCAS 调用：

```
cnc_allclibhndl3 cnc_freelibhndl cnc_statinfo cnc_machine cnc_rdcount cnc_rdaxisdata
cnc_rdparam cnc_rdtofs cnc_rdtofsinfo cnc_rdmacro cnc_rdalmmsg2 cnc_rdlife
cnc_rdexecprog cnc_exeprgname2 cnc_actf cnc_acts cnc_rdblkcount cnc_rdprogdir3
cnc_pdf_slctmain cnc_pdf_del cnc_download4 cnc_dwnstart4 cnc_dwnend4 cnc_upload4
cnc_upstart4 cnc_upend4 cnc_startupprocess cnc_exitprocess cnc_rdparam
```

方法名（`Focas::getPartCount` / `getAxisPosition` / `getToolParams` / `fileGetVal` /
`downloadProgram` …）与它们一一对应。也就是说：**这一家要么调 SDK，
要么按 01 册实现 FOCAS 线协议**，两条路都不需要现场抓包。

### 4.2 840D / i5：OPC UA 的节点是字面量 🟢

`libsinumerik-arm.so` 里直接留着 Siemens 的节点路径：

```
/Nck/State/numAlarms          /Nck/LastAlarm/textIndex[u1]
/Nck/LastAlarm/fillText1[u1]  …[u4]
/Channel/…  /Spindle/…        （其余按同样方式在 strings 里）
```

`libi5-opcua.so` 走 open62541（构建路径 `/home/bili_open/opcua_bak/open62541`）+
mbedtls 3.2.1，证书在 `nclink-service/conf/opcua_client_cert_and_priv_key/*.der`，
连接参数（`opc.tcp://host:4840`、`mode: anonymous`、`security_mode: None`、应用 URI
`urn:rpy:BigDogClient`）在 `i5_opcua_driver.json` 里。

### 4.3 GSK / KEDE / Mitsubishi-HTTP：项键齐，只差设备侧请求形状 🟡

三家都是自研 HTTP 客户端（`strings` 里能看到 `%s %s HTTP/1.1` 模板与
`GSKHTTP::httpPost` / `KEDEHTTP::httpPost`），项键与规整都拿到了：

```
GSK        /GSK/CNC/{Open/TCP,Close,STATUS,PROGRAM,LINE_NUMBER,PART_COUNT,
                     FEED_SPEED,FEED_OVERRIDE,SPDL_SPEED,SPDL_OVERRIDE,
                     RAPID_OVERRIDE,TOOL_NUMBER,WARNING}
KEDE       /KEDE/CNC/GNC62/{…AXIS_ACT_POSITION,TOOL_PARAM…}（14 个）
Mitsubishi /Mitsubishi/CNC/M70/{GetStatus,GetPartCount,GetProgramName,
                     GetRelativePositionX/Y/Z,GetTime*,ReadFile,WriteFile,
                     GetFileList,RemoveFile,GetWarning,…}（19 个）
```

缺的是"这些项键对应机床 HTTP 服务上的哪个 URL / 什么 JSON"——插件把这个藏在代码里，
`strings` 没留字面量。补法见 §6。

---

## 5 对 20/21/28 册的修正

| 之前的说法 | 现在的结论 |
|---|---|
| "GSK/科德/精雕/海康/华数/840D/i5/FOCAS 能力面与端点面已补，**报文级布局仍需抓包**" | 分开看：**FOCAS / 840D / i5 / 华数**连 SDK 或节点集都拿到了，**精雕 / 海康（相机）**要看 §6 或厂商文档；只有 **GSK / KEDE / Mitsubishi-HTTP** 真正缺"设备侧请求形状"，而这也能本地抓 |
| "20 册：科德/精雕/海康 资料 🔴 缺" | 科德有 `kede.json`（78 项）+ `libkede-http.so`（23 个方法 + 14 个项键）；精雕有 `jingdiao.json`（14 项）+ `hp2x_jd.lua`；相机有 `camera.json`（3 项）+ `libcamera.so`（28 个方法） |
| "现场只有 Go 符号名" | 插件 `.so` 的 `.dynsym` 完好，**C++ 方法签名全在**（§2） |

---

## 6 本地仿真抓包：已经在跑，战绩与卡点

**这条路走通了**：现场包里的二进制全是 **ELF32 ARM hard-float**（Go 网关、宿主、
插件都是），Docker Desktop 自带 `binfmt`，加一个 `--platform linux/arm/v7` 就能在
本机跑起来；再把 `ipAddress` 指向我们自己的"假机床"，设备侧报文就归我们了。
工具与复现命令在 [`tools/site-probe/`](../tools/site-probe/README.md)：

```sh
cd tools/site-probe && docker build --platform linux/arm/v7 -t ncl-arm-probe .
docker run --rm --platform linux/arm/v7 \
    -v <现场包>/app1/nclink-service:/svc:ro -v $PWD:/work \
    ncl-arm-probe sh /work/focas_run.sh
```

### 6.1 FOCAS2 握手 🟢 已抓到

`libfwlib32.so` 的 `cnc_allclibhndl3("127.0.0.1", 8193, 3)` 对假机床依次发
（两次 TCP 连接、四条消息）：

```
a0 a0 a0 a0 00 01 01 01 00 02 00 01     # 12 字节，第一条连接
a0 a0 a0 a0 00 01 01 01 00 02 00 02     # 12 字节，第二条连接（计数器 +1）
a0 a0 a0 a0 00 01 21 01 00 00           # 10 字节
a0 a0 a0 a0 00 01 02 01 00 00           # 10 字节
```

没有应答时返回 **-16（EW_SOCKET）** 并重试一次。要往下拿数据调用
（`cnc_statinfo` / `cnc_rdparam` / `cnc_rdaxisdata` …）得先把这四条回应答对；
应答只有两种来源：一台真 FANUC 抓一次，或按 Fwlib32 手册把应答字段补全。

### 6.2 插件 ABI 🟢 已解出

插件导出 4 个未修饰的 C 入口（`nm -D`）：

```c
void *create(nlohmann::json *params, spdlog::logger *logger);
int   call(void *instance, int op /*OperationType*/, const char *method,
           nlohmann::json *request, nlohmann::json *response,
           nlohmann::json *extra);
void  destroy(void *instance);
const char *get_version(void);
```

`call` 转发到的 C++ 虚函数在 `libbase.so` 里，修饰名是
`_ZN7BaseMod4callESs13OperationTypePN8nlohmann10basic_jsonISt3map…ES9_S9_`，
即 `BaseMod::call(std::string method, OperationType op, json*, json*, json*)`。
（**C 侧参数顺序是"实例、操作类型、方法名、请求 json、响应 json、附加 json"**；
第一版 harness 按 `(实例, 方法名, 参数文本, out)` 传，把 `const char*` 当枚举、
把参数文本当 json 解引用，直接段错误。）

三条硬约束（🟢 导入表/反汇编）：

1. **旧 string ABI**：插件导入的是 `_ZNSsC1EPKcRKSaIcE`（老 ABI 的 `std::string`），
   不是 `_ZNSt7__cxx1112basic_string…`；harness 必须
   `-D_GLIBCXX_USE_CXX11_ABI=0`，否则跨边界写坏堆（`free(): invalid pointer`）。
2. **自带第三方库**：HTTP 是 **cpp-httplib**（`httplib::Client::set_connection_timeout`
   等），日志是 **spdlog**（`spdlog::logger::debug<const char*&, const char*&>(
   fmt::v8::basic_format_string<…>)`），json 是 **nlohmann**，但它的 map 比较器是
   `std::less<std::string>`（修饰名 `St4lessISsE`），不是 3.11+ 的 `std::less<void>`。
3. **加载顺序**：`libstdc++.so.6` → `libbase64.so` → `libLogApi.so`
   （+`libboost_thread.so`）→ `libbase.so` → 具体插件。现场这些 `.so` 是 C++，但
   **NEEDED 里没有 libstdc++**，平时是宿主 `nclink-service` 把它拉起来的。

logger 可以造假：插件只拿它打日志，一个全零、虚表全是空函数的对象就够
（`plugin_drive.cpp` 的 `fake_logger_make()`）——用它可以一路走到
`ctor -> 0x4152bc`（实例真的造出来了）。

### 6.3 卡在哪（下一步）

现在能"加载 → 造实例 → 调 `call`"，卡在最后一步：**`call` 要的
`nlohmann::json*` 必须是插件那一版的对象**（`std::less<std::string>`，即
nlohmann < 3.11），我们手上是 3.11.2（`std::less<void>`），把我们的对象交给它
解引用就段错误。三条路：

1. 用插件那一版 nlohmann（试 3.9/3.10 的单头文件）编 harness，`json*` 就通了；
2. 跑现场宿主 `nclink-service`（它自己那版 json，天然对齐）——已试到中途：
   - ✅ 它能在 armv7 容器里起来；spdlog 报的
     `Failed getting file size from fd: Value too large for defined data type`
     是 32 位进程 `fstat()` 一个 Windows bind-mount 上的文件报 EOVERFLOW，
     把工作目录与日志放进容器自己的文件系统就好了；
   - ✅ `./nclink.cfg`（`-C nclink_cfg.json` 之外的老键值配置）的键名从二进制里
     抄出来了：`server_ip` / `server_port` / `reconnect_sec` / `user_name` /
     `password` / `log_level` / `log_file` / `log_limit` / `model` / `device_id`
     / 各主题的 `_qos`。空文件会让它 SIGBUS（读 0 字节文件），写全就不报打开了；
   - ❌ 之后仍是 qemu 下的 SIGBUS，崩得很早、日志一个字没写，这条路暂时不通。
3. 绕开 json：只调接受 JSON **文本**的入口（如
   `GSKHTTP::GSKHTTP(spdlog::logger*, std::string)`），但那样走不到业务逻辑。

任一条通了，GSK / KEDE / Mitsubishi-HTTP / 相机的设备侧请求形状就能在本地补齐。

### 6.4 网关路线：走通了（2026-09）🟢

上面第 2 条走不通，但换个对象就通了：**协议实现其实在 Go 网关里**。现场那台
`hp2x_box200`（:33123，"Communication Protocol Server"）自己实现各品牌协议，
C++ 插件只是 NC-Link 侧的适配器（它 POST 回网关）。而网关是**静态 Go 二进制**
——没有 C++ ABI 这回事，直接跑就行：

```sh
docker run --rm --platform linux/arm/v7 \
    -v <现场包>/app1/hp2x:/hp2x:ro -v tools/site-probe:/work \
    ncl-arm-probe sh /work/gateway_probe.sh
```

脚本做三件事：起一个假机床（`mock.py`，只记录）→ 起 `hp2x_box200` →
`POST /GSK/CNC/Open/TCP {"ipAddress":"127.0.0.1","port":6000,"timeout":5}`
拿到 `connectionId`，再逐项 `POST /GSK/CNC/<项> {"connectionId": …}`。
**设备侧请求就落在假机床的日志里**，一项一帧。

已经抓到 **GSK 的 11 条设备侧请求帧**（`STATUS` `0x11`、`PART_COUNT` `0x16`、
`PROGRAM` `0x12`、`LINE_NUMBER` `0x23`、`TOOL_NUMBER` `0x17`、`WARNING` `0x81`、
`0x1a`+子码的进给/主轴/倍率五条；帧头 `93`、帧尾 `55 aa`），见
[08 册 §4.5](08-GSK-广州数控.md)。

**到 2026-09 为止一共抓了 7 家**（都是同一套工具，每家一次跑）：

| 品牌 | 模块 | 设备侧形状 | 写在哪 |
|---|---|---|---|
| 广州数控 GSK | `GSK/CNC` | 二进制帧 `93…55 aa`，命令字节区分项 | [08 册 §4.5](08-GSK-广州数控.md) |
| 科德 GNC62 | `KEDE/CNC/GNC62` | 一行一个 XML（`<ncda>`/`<counter>`/`<feed>`…） | [20 册 §1.1](20-缺口-科德-精雕-海康.md) |
| 北京精雕 | `JINGDIAO/CNC` | 24 字节定长（自增序号 + 项代码） | [20 册 §2.1](20-缺口-科德-精雕-海康.md) |
| 三菱 M70 | `Mitsubishi/CNC/M70` | GIOP 80 字节，`mochaGetData(代码,索引)` | [05 册 §4.1](05-MITSUBISHI-CNC-M70-MELDAS.md) |
| 兄弟 Brother | `Brother/CNC` | ASCII `%<命令8><参数8>00\r\n…%` | [11 册 §3.1](11-BROTHER-兄弟.md) |
| 西门子 S7NCU | `S7NCU` | S7comm：Setup + Read Var（区域 `0x12` = SZL） | [03 册 §5.1](03-SIEMENS-S7-PLC.md) |
| FANUC 机器人 | `FANUC/ROBOT` | 56 字节定长，端口字区分（5000/6000/7000） | [02 册 §2.1](02-FANUC-机器人-RMI.md) |

**同一套办法对其它自研模块都成立**（改一下模块名与项键即可）：科德
`/KEDE/CNC/GNC62/*`、三菱 `/Mitsubishi/CNC/M70/*`、三菱 PLC
`/Mitsubishi/Plc/MC/*`、西门子 `/S7/*`、Modbus `/Modbus/*`、FANUC `/FOCAS/*`、
840D `/SINUMERIK/*`… 这等于把"设备侧请求形状"这一格的抓取工作变成了
**改个模块名跑一遍**。

还差的一小格：**应答格式**。把请求原样回给网关会被判
`unexpected response command`，说明应答里的"命令"要按它的规则构造；用同一个
脚本（`gateway_probe.sh <应答hex>`）迭代即可，网关的报错就是判据。

### 6.4.1 应答格式：已查到的与卡点（2026-09）

GSK 这一家专门查过一轮（工具：`gsk_reply_sweep.sh` / `gsk_reply_sweep2.sh`，
`mock.py` 支持 `@文件` 逐次读应答，`reply_sweep2` **每次都重新开连接**）：

🟢 **帧的框定规则**（把请求原样回给网关不会被拒之门外，只被判"命令不对"）：
帧头 `93`、第 2~3 字节是大端长度、第 4 字节（`body[0]`）必须 `00`（改它就 500）、
帧尾 `55 aa`；中间的会话/计数字段可以照抄请求。**逐个字节扫过请求帧的每一
位置**，只有第 0~3 与最后两字节会改变结果——也就是说**"命令"不在我们以为的
`body[9]`（请求里那个 `0x11`）上**。

🟡 **卡点**：能凑成"完整帧"的应答（不触发 EOF/panic）一律被判
`unexpected response command`，而且**与第 4~13 字节的取值无关**——判据不在我们
以为的 `body[9]`（请求里那个 `0x11`）上。三个探针依次排除：

1. `gsk_reply_sweep2.sh`：**每个候选重开连接**，逐字节扫请求帧的全部 16 个位置
   —— 只有 0~3 与 14~15 会改变结果（改这些会 EOF/panic），4~13 完全无影响；
2. `gsk_reply_sweep3.sh`（前半）：把 body 加长 2 字节（长度字段同步 +2），
   逐值扫新增的两字节 —— 仍一律 `unexpected response command`；
3. `gsk_reply_sweep3.sh`（后半）：**扫形状**——body 长度 1~14 × 末字节 8 个取值
   —— 短的（≤ 8）直接 500（等不到完整帧），长的全是同一句判定。

也就是说：帧框对了、内容随便填也过不了。剩下的可能只有"应答带值、命令位置随
长度走且需要一次给对整帧"或"判据用到请求侧的会话/序号"。

🟢 **静态线索**：网关的 Go 符号没 strip，能直接读出源文件名与函数名——
协议实现在 **`hp2x/protocols/gsk/cnc/gskcnc.go`**，HTTP 面在
**`hp2x/httphandlers/gsk/cnc/handler.go`**（函数如 `(*GskCnc).GetStatus`）。
错误串 "unexpected response command" 在 .rodata `0x7fe926`，其地址在 .text 的
两处字面量被引用：**`0x61a3a0`、`0x6206ac`**（.text 的 vaddr = 文件偏移 +
0x10000）。顺着引用反汇编能看到构造错误的代码，但要读出"期望的命令常量"需要
先按 Go 的 pclntab 把函数边界解出来——这是一次正经的 Go 逆向，不是几步的事。

**结论与建议**：GSK 的应答格式**用黑盒扫不出来**（已排除掉所有单字节假设）。
两条确定的路：

1. **现场抓一次真机应答**——成本最低（请求侧我们已经有全套帧，抓包只差一台
   带 GSK 系统的机床），抓到后照抄最小可用应答即可；
2. 用 Ghidra（装 Go 插件）打开 `hp2x_box200`，直接读 `gskcnc.go` 里
   `(*GskCnc)` 的解析函数——pclntab 完好，函数名与源文件都在。

其余六家（科德/精雕/M70/兄弟/S7NCU/FANUC 机器人）的应答同理：**XML 那家
（科德）最容易**，因为网关的报错直接点名期望的元素名（`expected element type
<counter>`），照它回即可。

### 6.5 网关的全路由表（201 条）🟢

网关自带 OpenAPI（`/api.json`），一次 GET 就是**完整的能力面**——75 个分组、
201 条路由。复现：`tools/site-probe/gateway_routes.sh`。

```
ANCHUAN (4)     ROBOT/{Open/UDP,Close,GetData,GetWarn}
Brother (12)    CNC/{Open/TCP,Close,CD,LS,PWD,MakeDir,Delete,GetToolList,
                    GetCurProgName,GetMaintenanceData,ReceiveFileReq,SendFileReq}
Debug (4)       Open/TCP Close Send ShowClients
FANUC (21)      ROBOT/{Open/TCP,Init,Init2,Close,GetData,GetAlarmList,GetHexResponse,
                    ReadGI,ReadGO,ReadUI,ReadUO,ReadSI,ReadSO,ReadRDI,ReadRDO,
                    ReadSDI,ReadSDO,ReadPMCR2,PrvReadBit,PrvReadUWord,ReadRShort}
Foxconn (3)     Chengdu/Robot/Nachi/{Open/TCP,Close,GetResponse}
GSK (27)        CNC/{Open/TCP,Close,Init,STATUS,PROGRAM,FEED_SPEED,FEED_OVERRIDE,
                    SPDL_SPEED,SPDL_OVERRIDE,RAPID_OVERRIDE,LINE_NUMBER,PART_COUNT,
                    TOOL_NUMBER,TOOL_PARAM,COORDINATE,WARNING,C04,Attr,
                    AXIS_ACT_POSITION,AXIS_ACT_SPEED,AXIS_CMD_POSITION,AXIS_CMD_SPEED,
                    CD,LS,SendFile,ReceiveFile,Remove}
JINGDIAO (13)   CNC/{Open/TCP,Close,Bind,STATUS,PROGRAM,FEED_SPEED,FEED_OVERRIDE,
                    SPDL_SPEED,SPDL_OVERRIDE,LINE_NUMBER,PART_COUNT,WARNING,GetResponse}
KEDE (14)       CNC/GNC62/{Open/TCP,Close,STATUS,PROGRAM,FEED_SPEED,FEED_OVERRIDE,
                    SPDL_SPEED,SPDL_OVERRIDE,RAPID_OVERRIDE,LINE_NUMBER,PART_COUNT,
                    AXIS_ACT_POSITION,TOOL_PARAM,WARNING}
LSV2 (22)       Open/TCP Close GetVersion GetExecutionStatus GetProgramStatus
                GetProgramStack GetAxesLocation GetSpindleToolStatus GetOverrideInfo
                GetErrorMessages GetSystemParameter ReadPLC GetFileList GetFileInfo
                GetDirectoryContent GetDirectoryInfo ChangeDirectory MakeDirectory
                DeleteFile DeleteEmptyDirectory SendFile ReceiveFile
Lua (4)         Start Stop Status Tasks
MTConnect (3)   Open/TCP Close Data
Mitsubishi (28) CNC/M70/{Open/TCP,Init,Close,GetStatus,GetPartCount,GetProgramName,
                    GetLineNumber,GetFeedSpeed,GetWarning,GetRelativePositionX/Y/Z,
                    GetTimePowerOn,GetTimeMachining,GetTimeCumulative,
                    GetFileList,ReadFile,WriteFile,RemoveFile}
                Plc/MC/{Open/TCP,Close,Read,Write}   Plc/SLMP/{Open/TCP,Close,IsConnected,Read,Write}
Modbus (5)      Open/{TCP,RTU,ASCII} Function Close
S7 (4)          Open/TCP Read Write Close
S7NCU (25)      Open/TCP Close Alarm Program Execution Mode PlcType NckName NckNo
                NckVer CoordinateAbsolute CoordinateMachine CoordinateRelative
                CoordinateName FeedActual FeedSet FeedOverride SpeedActual SpeedSet
                SpeedOverride S1Load ToolNo PartCount CycleTime LastRunTime
SYNTEC (12)     CNC/{Open/TCP,Close,STATUS,PROGRAM,FEED_SPEED,FEED_OVERRIDE,
                    SPDL_SPEED,SPDL_OVERRIDE,LINE_NUMBER,PART_COUNT,WARNING,GetResponse}
```

两条值得记的：**FANUC 只有 `ROBOT` 分组**（CNC/FOCAS 不在网关里，走
`libfocas.so` + FANUC 自己的 `libfwlib32.so`）；**LSV2 有 22 个方法**、**S7NCU 有
25 个**，都远超我们此前从插件符号里看到的数量。

---

## 7 落地顺序建议

1. **先补数据面**（零风险、纯抄）：把 §4 的模型项与 §4.3 的项键并进 08/09/20/24/25
   等分册，把 `driver_defs` 的端口/超时/路径分派并进我们的 `conf/driver/*.json` 样例。
2. **再补 SDK 型驱动**：FOCAS（01 册 + 29 个 `cnc_*`）、840D/i5（OPC UA 节点）、
   华数（Hsc3 API 面）——这三家可以按 SDK 的语义直接写，不依赖抓包。
3. **最后补自研 HTTP 三家**：按 §6 本地抓一次，把 URL/JSON 定下来。

---

## 8 包里还带着**现场跑过的运行日志**（🟢）

`INCBOX200/log/` 里有这台盒子 2024-12-03 的真实日志：`nclink-service.log`
（793 KB）与两份轮转、`rmc.log`、`boxssdp.log`、`nclink.log`、`runlog`
（+`runlog_bak`，盒子的启动脚本输出）。它们回答了两件代码看不出来的事：

**一、数据项是"一项一个 data_driver"** —— 服务启动时逐项建驱动，
`nclink-service.log` 里 100 多条 `create data_driver of <项路径>`，例：

```
/NC_LINK_ROOT/MACHINE/AXIS@4/SERVO_DRIVER/POSITION
/NC_LINK_ROOT/MACHINE/AXIS@4/MOTOR/VARIABLE@LOAD
/NC_LINK_ROOT/MACHINE/AXIS@2/SCREW/SPEED
/NC_LINK_ROOT/MACHINE/AXIS@5/NUMBER
```

这正是我们 `ncl_adapter` 的点位表：**模型项 → 一条 driver 点位**。

**二、这台盒子当时没连上机床** —— 日志里 FOCAS 侧是清一色的

```
[error] getFocasHandle cnc_allclibhndl3, ip:192.168.1.100, port:8193, timeout:2
[error] Failed to Alloc Focas Handle: -16
```

2909 次（约每 1~1.5 秒一轮，与 §6.1 抓到的"失败重试一次"一致），另有 513 次
`pmcRapidOverride data error`（主轴倍率那条 PMC 读回的值不合法）。也就是说：
**现场那份日志里没有成功的报文样本**，字节级证据还得靠 §6 的本地仿真或另找一台
真机抓。这条否证很关键——不要以为包里的日志能直接当抓包用。
