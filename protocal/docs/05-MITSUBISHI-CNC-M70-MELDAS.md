# 05 · 三菱 CNC M70/M80（MELDAS / GIOP）★ 实现规格书

> **证据**：🟢 全实证 —— 用户交付包（EZSocketNc.h 13687 行 + MITSUBISHI_PROTOCOL-3 4894 行 + C# 交付 3519 行 + Protol.txt 抓包）
> **定位**：日系 CNC 中**唯一可绕开 SDK 授权自行实现**的协议（线协议已完全解开）

---

## 1. 速查

| 项 | 值 |
|---|---|
| 端口 | **TCP 683**（机床侧参数 #1929 可改） |
| 传输 | TCP，单会话独占 |
| 协议本质 | **CORBA GIOP 1.0 之上的私有 `mocha*` 操作集** |
| 报文长度 | 请求固定 **80 字节**（`mochaGetData` 系） |
| 字节序 | **小端**（GIOP flags=0x01） |
| 最小可用路径 | `mochaGetData(0x25, 子码=机械/工件/当前/剩余, 轴号)` → 坐标 |
| 机床侧配置 | #1925=1（网络使能）、#1926=IP、#1927=掩码、#1928=网关、#1929=端口；面板口令 `MPARA` |
| 现成参考 | EZSocket（COM，需授权）；`MITSUBISHI_PROTOCOL-3`（裸协议，无需授权） |

---

## 2. 连接建立

```
① TCP connect(192.168.x.x, 683)
② 无握手 —— 直接发 GIOP 请求报文
③ 会话保持：请求 ID 自增（4 字节），响应按 ID 匹配
④ 关闭：直接 TCP close 或发 mochaCancelModal
```

**注意**：机床侧仅在 `#1925=1` 时才监听；`#1929` 改端口后需重启网络。

---

## 3. 帧格式（字节级）

### 3.1 请求（80 字节定长）

```
偏移  长度  内容                典型值            说明
0     4     47 49 4f 50         "GIOP"            CORBA 魔数
4     2     01 00               1.0               版本
6     1     01                  —                 标志位（0x01 = 小端）
7     1     00                  —                 消息类型（0=Request, 1=Reply）
8     4     44 00 00 00         68                正文长度 = 总长 - 12（LE）
12    4     00 00 00 00         —                 保留
16    4     请求ID              任意               **LE，响应按此匹配**
20    4     01 ff ff ff         1                 response_expected=1 + 保留 3 字节
24    4     04 00 00 00         4                 对象键长度
28    4     01 00 00 00         1                 对象键
32    4     0d 00 00 00         13                操作名长度（含结尾 NUL）
36    12    "mochaGetData"      —                 操作名（12 字符）
48    4     00 00 00 03         —                 固定
52    4     00 00 00 00         —                 固定
56    4     命令码               见 §4              **LE**
60    4     子码                 见 §4              **LE**
64    4     数量                 1                 读取元素个数
68    4     地址/轴号             见 §5              **LE**
72    4     00 00 00 00         —                 保留
76    4     期望返回类型          见 §3.3            **LE**
```

### 3.2 响应

```
0..7   47 49 4f 50 01 00 01 01     末字节 01 = Reply
16..19 请求 ID（与请求一致）
28..30 "IDL"                       无数据标志
36..    数据区（按 data[28] 类型标记 / data[32] 尺寸解析）
```

### 3.3 值类型编码

| 请求侧（期望类型） | 响应侧解析 | 长度 | 数据位置 |
|---|---|---|---|
| `0x01` | BYTE | 1 | `[36]` |
| `0x02` | INT16 | 2 | `[36:38]` LE |
| `0x03` | INT32 | 4 | `[36:40]` LE |
| `0x05` | DOUBLE(8，IEEE754) | 8 | `[36:44]` LE |
| `0x06` | DOUBLE(10，扩展) | 16 | `[44:52]`（前 8 字节对） |
| `0x10` | CString | — | 长度 `[36]`，字符从 `[40]` 起 |
| `0xbc` | CString-188 | — | 同上 |
| `0x49 0x44 0x4c`("IDL") | 无返回 | — | — |

---

## 4. 命令码表（双证据：源码注释 + C# 交付实测）

### 4.1 各数据项的实测请求帧（🟢 本地仿真抓取，2026-09）

现场交付包的 Go 网关自己实现了 M70 协议。把 `/Mitsubishi/CNC/M70/Open/TCP`
指向假机床（端口 683），再逐项 POST，就能拿到每项的真实 GIOP 请求
（复现：`tools/site-probe/gateway_probe.sh`）。**每项一帧、80 字节定长**
（报警那条 84 字节，方法名更长），与 §3.1 的布局完全一致：

```
47 49 4f 50 01 00 01 00 | 44 00 00 00 | 00 00 00 00 | <请求ID> | 01 00 00 00
04 00 00 00 01 00 00 00 | 0d 00 00 00 | "mochaGetData\0" | 00 | <代码> 00 00 00
00 00 00 00 | <参数1> | <参数2>
```

各 `mochaGetData` 项的**代码与参数**（小端 4 字节，偏移 49 起）：

| 项 | 代码 | 其后两个参数 |
|---|---|---|
| `GetStatus` | `ED ED 05 00` → 0x0005EDED | 35（0x23）、20（0x14） |
| `GetPartCount` | `5F 5A 06 00` → 0x00065A5F | 35、22（0x16） |
| `GetProgramName` | `00 00 21 00` → 0x00210000 | 126（0x7E）、8002（0x1F42） |
| `GetLineNumber` | `39 6D 06 00` → 0x00066D39 | 45（0x2D）、101（0x65） |
| `GetFeedSpeed` | `00 00 00 00` → 0 | 45、103（0x67） |
| `GetTimePowerOn` | `B8 62 00 00` → 0x000062B8 | 40（0x28）、1 |
| `GetTimeMachining` | 同上 0x000062B8 | 40、3 |
| `GetTimeCumulative` | 同上 0x000062B8 | 40、2 |
| `GetRelativePositionX` | 见下（请求ID 段与其它项排布不同） | 37（0x25）、3、1、16 |
| `GetWarning` | 方法名 **`mochaGetCurrentAlarmMsgFirst`**（0x1d=29 字节） | 1、10 |

`GetRelativePositionX` 那条的原始帧（84 字节那类的前半不同，逐字抄录）：

```
47 49 4f 50 01 00 01 00 44 00 00 00 00 00 00 00
24 c1 00 00 01 00 55 05 04 00 00 00 01 00 00 00
0d 00 00 00 6d 6f 63 68 61 47 65 74 44 61 74 61 00
00 00 00 00 00 00 00 00 25 00 00 00 03 00 00 00
01 00 00 00 01 00 00 00 00 00 00 00 10 00 00 00
```

结论：**M70 的数据项 = `mochaGetData(代码, 索引…)` 的两个整数**，Y/Z 轴与
其余时间项按同样办法再抓一次即可补齐（`gateway_probe.sh` 换个项名就行）。
应答侧的字段语义（值怎么编码）见 §4.1.3——**已闭环**。

#### 4.1.1 应答侧实测（🟢 2026-09 第二轮，`tools/site-probe/m70_reply_probe.sh`）

手法：**把网关自己的请求回声回去**（`EREP:`），只改必要字节——请求 ID、长度、
对象键全都不用猜；`mock.py` 为此加了 `cut:N`（回声后截断到 N 字节）。

| 试法 | 结果 | 说明 |
|---|---|---|
| 回声 + `[7]=01`（消息类型=Reply） | `error response length` | 长度检查先挂 |
| 回声 + 截断到 **40** 字节 | `error response data` | **长度检查过了**，数据检查挂 → 说明"对"的长度是 40 |
| 回声 + 截断到 36/44/…/84 | 全部 `error response length` | 只有 40 能过长度检查 |
| 改 `[8..11]`（GIOP size）三种值 | `length` / `data` | size 不是这条检查的开关 |
| `GetProgramName`（回声，不截断） | **`'aGetDat'`** | ✅ **CString 取在 `[40]` 起**，与 §3.3 完全一致 |
| `GetStatus`（回声，逐字节改 `[36..48]`） | 恒 `'free'` | 这个字节区间不参与状态判定（或走默认分支） |

两条错误串都在 `hp2x/common` 里（`go_pclntab.py --xstring` 反查到引用者是
`hp2x/common.init`，即包级错误变量）：`error response length` = 传输层长度不符，
`error response data` = 长度对了但数据区不符。

#### 4.1.2 应答闭环：CString 全通了（🟢）

`GetProgramName` 那条 panic 把布局直接交了出来：

```
HTTP 500 exception recovered: runtime error: slice bounds out of range
[:1751347093] with capacity 64
```

1751347093 = 40 + 0x68636F6D（回声里 `[36..39]` = `6d 6f 63 68` = "moch" 的 LE），
即代码做的是 **`data[40 : 40+len]`**，`len` 取自 `[36..39]`（LE uint32），字符从
`[40]` 起——**§3.3 的 CString 那一行完全正确**。顺着造应答即闭环：

| 造的应答 | 网关返回 |
|---|---|
| `[36..39]=7`、`[40..46]="ABCDEFG"` | **`'ABCDEFG'`** ✅ |
| `[36..39]=0` | `''` ✅ |
| 不打补丁（长度字段 = "moch" 的 ASCII） | `slice bounds out of range [:1751347093]`（就是上面那条 panic） |

顺带两个副产品：① `GetResponse3` 的缓冲区 **capacity = 64**；② "IDL 标记"
（`[28..30]="IDL"`）打不打都不影响 CString 的解析。

数值类的"最后一格"见 §4.1.3（已闭环）。

#### 4.1.3 数值类全部闭环：应答模板规则（🟢 2026-09 第三轮）

反汇编 `(*MitsubishiCncM70).GetPartCount`（`0x6355f8-0x635bb4`）把"最后一步"读透：

```
635864  bl hp2x/common.(*NetConn).GetResponse3   ; 收包
6359c4  响应长度 == 驱动自己拼的应答模板长度      ; 不等 → "error response length"
635a10  bl runtime.memequal                      ; 比模板前 N 字节，不等 → "error response data"
635a20  runtime.newobject + 组装 *bytes.Reader   ; 窗口 = 响应 [36..40)
635ac4  bl encoding/binary.Read                  ; 目标 = 0x749e48 = uint32（size=4, kind=10）
635ac8  成功路径
```

即：**网关比的是一个"应答模板"**（`bytes.Buffer` 里先写 24 字节 GIOP 头、再写每项
固定的尾巴，共 40/52/544 字节），响应必须**逐字节相同**（末 4 或 8 字节是取值槽，
不参与比较）。模板的头 24 字节 = **请求的前 24 字节**，只改 3 处：

| 偏移 | 应答要写 | 请求里的值 |
|---|---|---|
| 7 | `01`（消息类型 = Reply） | `00` |
| 8..11 | 总长 − 12（LE） | `44 00 00 00`（68） |
| 20..23 | `00 00 00 00` | 每项不同的运行时值（如 `01 5d 34 77`） |

`[12..15]` 两边都是 0；`[16..19]` 两边同值 = **数据项码**（工件计数 `3c 1f`、行号
`d0 00`、进给/坐标 `24 c1`、时间 `b8 62`、报警 `3c 1f`）。所以造应答最省事的办法
就是 `EREP:` 回声请求 + 打补丁（`m70_num_probe*.sh` 就是这么干的）。

**各数据项的应答（都已实测闭环）**：

| 数据项 | 请求方法 | 码 `[16..19]` | 应答总长 | 固定尾巴（`[24..]`） | 取值槽 | 实测 |
|---|---|---|---|---|---|---|
| 工件计数 `GetPartCount` | `mochaGetData` | `3c 1f 00 00` | 40 | `00 00 00 00 03 00 00 00 04 00 00 00` | **u32 @36** | 回 7/1234/1751347053 即得同值 ✅ |
| 行号 `GetLineNumber` | `mochaGetData` | `d0 00 00 00` | 40 | 同上 | **u32 @36** | 回 7/1234/10 ✅ |
| 开机/累计/加工时间 `GetTime*` | `mochaGetData` | `b8 62 00 00` | 40 | 同上 | **u32 @36**（秒，`0xFFFFFFFF` → 4294967295） | 三项各回 3600 ✅ |
| 状态 `GetStatus` | `getStartStatus` + `getPauseStatus` | `3c 1f 00 00` | 40 ×2 | 同上 | **u32 @36**（1 = 是） | 见下表 ✅ |
| 程序名 `GetProgramName` | `mochaGetData` | — | **40 + len** | 长度 `[36..39]`、字符 `[40..]` | CString | `'ABCDEFG'` ✅（§4.1.2） |
| 进给速度 `GetFeedSpeed` | `mochaGetData` | `24 c1 00 00` | 52 | `00 00 00 00 06 00 00 00 10 00 00 00 07 00 02 00 00 00 00 00` | **double @44** | 回 123.5 / −1.25 ✅ |
| 相对坐标 `GetRelativePositionX/Y/Z` | `mochaGetData` | `24 c1 00 00` | 52 | 同上，但 `[36..39] = 05 00 03 00` | **double @44** | X/Y/Z 各回 12.5 ✅ |
| 报警 `GetWarning` | `mochaGetCurrentAlarmMsgFirst` | `3c 1f 00 00` | **544**（`[8..11]=14 02 00 00`） | 载荷全 0 = 无报警 | — | 返回 `[]` ✅ |

**进给/坐标那 4 个字节是判定位**：`[36..39]` 换成另一项的值就报
`error response data`（`07000200` 只喂得动 `GetFeedSpeed`，`05000300` 只喂得动
`GetRelativePosition*`），所以这四个字节是"这一帧是哪个数据项的应答"的第二重校验。

**状态 `GetStatus`**：网关连发两帧查询（先 `getStartStatus` 再 `getPauseStatus`，
请求方法名同为 `mochaGetData`、码同为 `3c 1f`，靠请求 `[0x38]` 的 `0x14`/`0x16`
区分），两帧都回同值时：

| start | pause | 返回 |
|---|---|---|
| 1 | 0 | `'running'` |
| 0 | 0 | `'free'` |
| 0 / 1 | 1 | `'holding'` |
| 其它（任一查询失败） | | `'free'`（枚举 0） |

**时间三项的选择位**：请求 `[0x38]`（=偏移 56）的 u32 = 1/2/3 分别对应
`GetTimePowerOn`/`GetTimeCumulative`/`GetTimeMachining`，三者的应答模板完全相同。

**顺带修正 §4.1.1 的一处理解**：请求里那些"看起来固定的字节"其实多半是运行时值。
反汇编里能读到一份"骨架字面量"（`7e 00 00 00 42 1f 00 00 …`），但真机发出来的是
`02 00 00 00 64 00 00 00 …`、工件计数码 `e4 ed 05 00`、进给码 `1b 55 05 00`——骨架
只是模板，逐项数据（轴号/速率/数据码）在发送前被打进去。**要请求的准确字节，抓
mock 的日志比读反汇编可靠**。

探针：`tools/site-probe/m70_num_probe.sh`（工件计数）、`m70_num_probe2.sh`
（行号/进给/坐标 + 判定位互斥性）、`m70_num_probe3.sh`（时间/状态）、
`m70_status_warn_probe.sh`（状态四组合 + 报警）。`mock.py` 这一轮新增
`ZREP:N:off:hex,…`（零填充 N 字节再补丁）和 `SEQ:` 里可嵌套其它规格。

| 命令码 | 子码 | 语义 | 返回类型 | 备注 |
|---|---|---|---|---|
| `0x01` | — | 读系统信息（CNC 型号） | CString | 设定速度（写）也走此码 |
| `0x02` | — | 系统类型 | BYTE | — |
| `0x03` | `0x02` | **机械坐标** | DOUBLE | 见 §5 轴号 |
| `0x03` | `0x03` | **当前位置** | DOUBLE | — |
| `0x03` | `0x06` | **剩余距离** | DOUBLE | — |
| `0x03` | `0x08` | 零点偏置（G54） | DOUBLE | — |
| `0x03` | `0x0e` | 补正量（刀补） | DOUBLE/INT32 | — |
| `0x03` | `0x12` | 刀具寿命 | INT32 | — |
| `0x03` | `0x29` | G 代码组 | — | 同 `0x29` |
| `0x03` | `0x3b` | 伺服监控 | — | 负载/电流 |
| `0x03` | `0x7e` | 加工参数 | — | — |
| `0x03` | `0x7f` | 参数 | — | — |
| `0x04` | — | **变量读写**（公共变量，读/写） | `0x05` | 读=GetData，写=SetData |
| `0x06` | — | 局部变量 | — | — |
| `0x0b` | — | 公共变量 | — | — |
| `0x21` | `1` | **进给速度（实际）** | CString | C# `GetFeedRate` 实测 |
| `0x23` | `0x0a` | **运行状态** | BYTE | C# `GetStatus` |
| `0x23` | `0x0b` | **工作模式** | BYTE | C# `GetMode` |
| `0x25` | `1` | **工件坐标** | CString | 子码即坐标类型 |
| `0x25` | `2` | **机械坐标** | CString | — |
| `0x25` | `3` | **当前位置** | CString | — |
| `0x25` | `6` | **剩余距离** | CString | — |
| `0x28` | `1` | 通电时间 | INT32 | 秒 |
| `0x28` | `2` | 运行时间 | INT32 | 秒 |
| `0x29` | — | 读代码组（G/D/H） | CString | — |
| `0x2a` | `1` | 进给速度（设定） | CString | — |
| `0x2b` | `1` | 主轴转速（设定） | INT32 | — |
| `0x2b` | `101` | **刀具号** | INT32 | C# `GetToolNo` |
| `0x2d` | `101` | **程序名 / 程序号** | CString | C# `GetProgName` |
| `0x35` | — | 读 I/O BIT | BYTE | — |
| `0x36` | — | **读 I/O BYTE** | BYTE | C# `GetXInput` |
| `0x37` | — | 读 I/O WORD | INT16 | — |
| `0x3b` | — | 读伺服监控 | — | — |
| `0x3f` | `3` | **主轴转速** | INT32 | C# `GetSpindleSpeed` |
| `0x3f` | `4` | **主轴负载** | INT32 | C# `GetSpindleLoad` |
| `0x3f` | `201` | **主轴电流** | INT32 | C# `GetSpindleCurrent` |
| `0x3f` | `221` | **主轴温度** | INT32 | C# `GetSpindleTemp` |
| `0x7e` | `8002` | **工件计数** | INT32 | C# `GetProducts` |
| `0x7f` | — | 读参数 | — | — |

### 4.1 报警

| 操作 | 说明 |
|---|---|
| `mochaGetCurrentAlarmMsgFirst` | 取当前报警（首条） |
| `NcSystem_GetAlarm(消息号, 报警类型)` | 按索引遍历 |

**报警类型位**：`0x100` NC 报警 · `0x200` 停止码 · `0x300` PLC 报警 · `0x400` 操作员消息 · `0x500` 警告
**细分**：`0x101` 系统 · `0x102` 伺服 · `0x103` MCP · `0x104` 基础 PLC · `0x105` 用户 PLC · `0x106` 程序 · `0x107` 伺服警告 · `0x108` MCP 警告 · `0x109` 系统警告 · `0x10A` 操作错误 · `0x10B` 操作员错误

### 4.2 文件（程序上下载）

| 操作 | 说明 |
|---|---|
| `mochaFSOpenFile` | 打开（路径 `M01:\PRG\USER\xxxx`） |
| `mochaFSReadFile` | 读内容（分批） |
| `mochaFSCloseFile` | 关闭 |
| `mochaFSOpenDirectory` / `mochaFSReadDirectory` / `mochaFSCloseDirectory` | 目录枚举 |
| `mochaFSStatFile` | 文件属性 |
| `mochaFSRemoveFile` | 删除（**危险**） |
| `mochaFSIoctlFile` | IO 控制（程序信息） |
| `mochaSetData` | 通用写 |

**这一层的应答帧（🟡 2026-09 反汇编 + 假机床实测，`m70_fs_probe*.sh`）**：

和 §4.1.3 同构——24 字节 GIOP 头 + 16 字节块 + **载荷**，但**不比对模板**
（没有 `memequal`），只看两件事：

| 检查 | 位置 | 说明 |
|---|---|---|
| 长度 ≥ 24 / ≥ 40 | `mochaFSReadDirectory` `0x63a144`/`0x63a178` | < 24 或 < 40 直接报错 |
| 状态码 | 应答 `[20..23]`（LE u32） | **0 = 正常**，非 0 = 出错（`0x63a170`） |
| 载荷 | 应答 `[40..]` | 从 `[40]` 起**扫到第一个 `00`** 为止当文件名（`0x63a294` 的循环） |

目录枚举的会话顺序（`GetFileList`，`0x638b3c`）：
`mochaFSStatFile` → `mochaFSOpenDirectory` → `mochaFSReadDirectory`（**循环**，
每轮 append 一个文件名）→ 出错时 `mochaFSCloseDirectory` 并返回已收集的列表。

实测（假机床）：

| 试法 | 结果 |
|---|---|
| 全部回"40 字节、状态 0、载荷空" | `GetFileList` **一直轮询 `mochaFSReadDirectory`**（空名字不算结束） |
| 载荷给 `O1000`（`[40..44]`，无 `00` 结尾） | 名字取到（扫到载荷末尾），但下一帧出错后 http 层交付 `null` |
| 状态码改 1 | `GetFileList` → `null`（`GetWarning` 同款错误路径） |
| `ReadFile` 回"状态 0、载荷 `HELLO`" | 返回 `''`（内容不在 `[40..]`） |

**未闭环**：`GetFileList`/`ReadFile` 的**载荷**约定（文件名/文件内容具体放哪、
`[24..39]` 那 16 字节里哪一格是"还有下一条"）——试了 `[24..39]` 四格置 1 都不
结束循环。要一次真机的"列目录/读文件"会话抓包（已记入 31 册）。

---

## 5. 轴号与坐标类型

**轴号**（C# 交付实测）：`X=1, Y=2, Z=4`（位编码，第 4 轴=8，第 5 轴=16）
> C++ 样例用枚举 `AXIS_X=1,AXIS_Y=2,AXIS_Z=3,AXIS_C=4` —— **两者不一致**，以机床实测为准（建议做成可配置）。

**坐标类型**（命令 `0x25` / `0x03` 的子码）：`1`=工件坐标 · `2`=机械坐标 · `3`=当前位置 · `6`=剩余距离

---

## 6. 读写流程

### 6.1 读机械坐标（X 轴）

```
request = bytes([
  0x47,0x49,0x4f,0x50, 0x01,0x00,0x01,0x00,
  0x44,0x00,0x00,0x00,            # 长度 68
  0x00,0x00,0x00,0x00,
  id&0xff,(id>>8)&0xff,(id>>16)&0xff,(id>>24)&0xff,   # 请求 ID
  0x01,0xff,0xff,0xff,
  0x04,0x00,0x00,0x00,
  0x01,0x00,0x00,0x00,
  0x0d,0x00,0x00,0x00,
  ord('m'),ord('o'),ord('c'),ord('h'),ord('a'),ord('G'),ord('e'),ord('t'),ord('D'),ord('a'),ord('t'),ord('a'),
  0x00,0x00,0x00,0x03,
  0x00,0x00,0x00,0x00,
  0x25,0x00,0x00,0x00,            # 命令：读坐标
  0x02,0x00,0x00,0x00,            # 子码：机械坐标
  0x01,0x00,0x00,0x00,            # 数量 1
  axis,0x00,0x00,0x00,            # 轴号
  0x00,0x00,0x00,0x00,
  0x10,0x00,0x00,0x00])           # 期望 CString
```

### 6.2 响应解析（C# 交付的 CheckFrame）

```
if len(resp) < 33:                 error(-1) 长度不足
if resp[0:8] != 47 49 4f 50 01 00 01 01:  error(-2) 报文头异常
if int32_le(resp[16:20]) != id:    error(-3) ID 不匹配
if resp[28:31] == "IDL":           无数据
else: 按 resp[28] 类型标记解析（见 §3.3）
```

---

## 7. 错误码

**设备层**（`ME_DEV_*`，响应内的状态码）
| 值 | 含义 | | 值 | 含义 |
|---|---|---|---|---|
| 0 | OK | | 21 | 数据错 |
| 1 | 已打开 | | 22 | 已取消 |
| 2 | 未打开 | | 23 | 尺寸非法 |
| 4 | 卡不存在 | | 24 | 任务退出 |
| 6 | 通道错误 | | 25 | 硬件错误 |
| 7 | 句柄错误 | | 26 | 系统停机 |
| 8 | 无法打开 | | 50 | 未知功能 |
| 10 | 未连接 | | 51 | 设定值错误 |
| 11 | 未关闭 | | 20 | **超时** |

**SDK 层**：`EZ_ERR_NOT_OPEN=0x80A00101` … `EZ_ERR_OPEN_COMM=0x80A0010C`（通用）· `EZNC_COMM_* = 0x80B00301-0305`（通信）· `EZNC_FILE_* = 0x80B00201-0210`（文件）

---

## 8. 实现坑

1. **长度字段是「总长-12」**，不是总长。写错设备直接不回。
2. **操作名长度 13 但字符串只有 12 字节**（含结尾 NUL），后面 4 字节 `00 00 00 03` 是固定填充 —— 抄样例别自己推。
3. **轴号 1/2/4 是位编码**，不是序号。
4. **`0x03` 命令的多义性**：它是通用"设备数据"命令，语义完全由子码决定（0x02 机械坐标 vs 0x7e 加工参数），实现时必须 `(cmd, sub)` 联合判断。
5. **CString 返回**：长度在 `[36]`，内容从 `[40]` 起 —— 注意偏移 4 字节的空档。
6. **单会话独占**：异常退出后必须重连，旧会话可能残留（机床侧连接数有限）。
7. **写操作风险**：`mochaSetData` 可改参数/变量；`mochaFSRemoveFile` 删程序 —— 生产环境默认禁用。

---

## 9. 参考实现

| 来源 | 说明 |
|---|---|
| EZSocket OLE 封装宏 + 样例 | 未随本目录提供 |
| `MITSUBISHI_PROTOCOL-3`（交付包） | 裸协议 C++ 实现（110 函数，报文逐字节现成） |
| C# 交付 `Form1.cs` | 19 数据点 Socket 直连实现（可直接移植） |
| EZSocket（COM） | 官方 SDK：`CLSIDFromProgID("EZSocketNc.EZNcCommunication")` → M70 用 `CLSID_...270` → `SetTCPIPProtocol(ip, port)` → `Open2(EZNC_SYS_MELDAS700M, 1, 100)`（**需授权**） |
| 完整 API 表 | 39 接口 / 271 方法 / CLSID 版本表 / 错误码全集（原始素材未随本目录提供） |
