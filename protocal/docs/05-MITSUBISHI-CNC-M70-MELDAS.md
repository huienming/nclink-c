# 05 · 三菱 CNC M70/M80（MELDAS / GIOP）★ 实现规格书

> **证据**：🟢 全实证 —— 用户交付包（EZSocketNc.h 13687 行 + MITSUBISHI_PROTOCOL-3 4894 行 + C# 交付 3519 行 + Protol.txt 抓包）
> **定位**：日系 CNC 中**唯一可绕开 SDK 授权自行实现**的协议（线协议已完全解开）

---

## 1. 速查

| 项 | 值 |
|---|---|
| 端口 | **TCP 683**（机床侧参数 #1929 可改） |
| 传输 | TCP，单会话独占 |
| 协议本质 | **CORBA GIOP 1.0 之上的私有 `mocha*` 操作集** |
| 报文长度 | 请求固定 **80 字节**（`mochaGetData` 系） |
| 字节序 | **小端**（GIOP flags=0x01） |
| 最小可用路径 | `mochaGetData(0x25, 子码=机械/工件/当前/剩余, 轴号)` → 坐标 |
| 机床侧配置 | #1925=1（网络使能）、#1926=IP、#1927=掩码、#1928=网关、#1929=端口；面板口令 `MPARA` |
| 现成参考 | EZSocket（COM，需授权）；`MITSUBISHI_PROTOCOL-3`（裸协议，无需授权） |

---

## 2. 连接建立

```
① TCP connect(192.168.x.x, 683)
② 无握手 —— 直接发 GIOP 请求报文
③ 会话保持：请求 ID 自增（4 字节），响应按 ID 匹配
④ 关闭：直接 TCP close 或发 mochaCancelModal
```

**注意**：机床侧仅在 `#1925=1` 时才监听；`#1929` 改端口后需重启网络。

---

## 3. 帧格式（字节级）

### 3.1 请求（80 字节定长）

```
偏移  长度  内容                典型值            说明
0     4     47 49 4f 50         "GIOP"            CORBA 魔数
4     2     01 00               1.0               版本
6     1     01                  —                 标志位（0x01 = 小端）
7     1     00                  —                 消息类型（0=Request, 1=Reply）
8     4     44 00 00 00         68                正文长度 = 总长 - 12（LE）
12    4     00 00 00 00         —                 保留
16    4     请求ID              任意               **LE，响应按此匹配**
20    4     01 ff ff ff         1                 response_expected=1 + 保留 3 字节
24    4     04 00 00 00         4                 对象键长度
28    4     01 00 00 00         1                 对象键
32    4     0d 00 00 00         13                操作名长度（含结尾 NUL）
36    12    "mochaGetData"      —                 操作名（12 字符）
48    4     00 00 00 03         —                 固定
52    4     00 00 00 00         —                 固定
56    4     命令码               见 §4              **LE**
60    4     子码                 见 §4              **LE**
64    4     数量                 1                 读取元素个数
68    4     地址/轴号             见 §5              **LE**
72    4     00 00 00 00         —                 保留
76    4     期望返回类型          见 §3.3            **LE**
```

### 3.2 响应

```
0..7   47 49 4f 50 01 00 01 01     末字节 01 = Reply
16..19 请求 ID（与请求一致）
28..30 "IDL"                       无数据标志
36..    数据区（按 data[28] 类型标记 / data[32] 尺寸解析）
```

### 3.3 值类型编码

| 请求侧（期望类型） | 响应侧解析 | 长度 | 数据位置 |
|---|---|---|---|
| `0x01` | BYTE | 1 | `[36]` |
| `0x02` | INT16 | 2 | `[36:38]` LE |
| `0x03` | INT32 | 4 | `[36:40]` LE |
| `0x05` | DOUBLE(8，IEEE754) | 8 | `[36:44]` LE |
| `0x06` | DOUBLE(10，扩展) | 16 | `[44:52]`（前 8 字节对） |
| `0x10` | CString | — | 长度 `[36]`，字符从 `[40]` 起 |
| `0xbc` | CString-188 | — | 同上 |
| `0x49 0x44 0x4c`("IDL") | 无返回 | — | — |

---

## 4. 命令码表（双证据：源码注释 + C# 交付实测）

| 命令码 | 子码 | 语义 | 返回类型 | 备注 |
|---|---|---|---|---|
| `0x01` | — | 读系统信息（CNC 型号） | CString | 设定速度（写）也走此码 |
| `0x02` | — | 系统类型 | BYTE | — |
| `0x03` | `0x02` | **机械坐标** | DOUBLE | 见 §5 轴号 |
| `0x03` | `0x03` | **当前位置** | DOUBLE | — |
| `0x03` | `0x06` | **剩余距离** | DOUBLE | — |
| `0x03` | `0x08` | 零点偏置（G54） | DOUBLE | — |
| `0x03` | `0x0e` | 补正量（刀补） | DOUBLE/INT32 | — |
| `0x03` | `0x12` | 刀具寿命 | INT32 | — |
| `0x03` | `0x29` | G 代码组 | — | 同 `0x29` |
| `0x03` | `0x3b` | 伺服监控 | — | 负载/电流 |
| `0x03` | `0x7e` | 加工参数 | — | — |
| `0x03` | `0x7f` | 参数 | — | — |
| `0x04` | — | **变量读写**（公共变量，读/写） | `0x05` | 读=GetData，写=SetData |
| `0x06` | — | 局部变量 | — | — |
| `0x0b` | — | 公共变量 | — | — |
| `0x21` | `1` | **进给速度（实际）** | CString | C# `GetFeedRate` 实测 |
| `0x23` | `0x0a` | **运行状态** | BYTE | C# `GetStatus` |
| `0x23` | `0x0b` | **工作模式** | BYTE | C# `GetMode` |
| `0x25` | `1` | **工件坐标** | CString | 子码即坐标类型 |
| `0x25` | `2` | **机械坐标** | CString | — |
| `0x25` | `3` | **当前位置** | CString | — |
| `0x25` | `6` | **剩余距离** | CString | — |
| `0x28` | `1` | 通电时间 | INT32 | 秒 |
| `0x28` | `2` | 运行时间 | INT32 | 秒 |
| `0x29` | — | 读代码组（G/D/H） | CString | — |
| `0x2a` | `1` | 进给速度（设定） | CString | — |
| `0x2b` | `1` | 主轴转速（设定） | INT32 | — |
| `0x2b` | `101` | **刀具号** | INT32 | C# `GetToolNo` |
| `0x2d` | `101` | **程序名 / 程序号** | CString | C# `GetProgName` |
| `0x35` | — | 读 I/O BIT | BYTE | — |
| `0x36` | — | **读 I/O BYTE** | BYTE | C# `GetXInput` |
| `0x37` | — | 读 I/O WORD | INT16 | — |
| `0x3b` | — | 读伺服监控 | — | — |
| `0x3f` | `3` | **主轴转速** | INT32 | C# `GetSpindleSpeed` |
| `0x3f` | `4` | **主轴负载** | INT32 | C# `GetSpindleLoad` |
| `0x3f` | `201` | **主轴电流** | INT32 | C# `GetSpindleCurrent` |
| `0x3f` | `221` | **主轴温度** | INT32 | C# `GetSpindleTemp` |
| `0x7e` | `8002` | **工件计数** | INT32 | C# `GetProducts` |
| `0x7f` | — | 读参数 | — | — |

### 4.1 报警

| 操作 | 说明 |
|---|---|
| `mochaGetCurrentAlarmMsgFirst` | 取当前报警（首条） |
| `NcSystem_GetAlarm(消息号, 报警类型)` | 按索引遍历 |

**报警类型位**：`0x100` NC 报警 · `0x200` 停止码 · `0x300` PLC 报警 · `0x400` 操作员消息 · `0x500` 警告
**细分**：`0x101` 系统 · `0x102` 伺服 · `0x103` MCP · `0x104` 基础 PLC · `0x105` 用户 PLC · `0x106` 程序 · `0x107` 伺服警告 · `0x108` MCP 警告 · `0x109` 系统警告 · `0x10A` 操作错误 · `0x10B` 操作员错误

### 4.2 文件（程序上下载）

| 操作 | 说明 |
|---|---|
| `mochaFSOpenFile` | 打开（路径 `M01:\PRG\USER\xxxx`） |
| `mochaFSReadFile` | 读内容（分批） |
| `mochaFSCloseFile` | 关闭 |
| `mochaFSOpenDirectory` / `mochaFSReadDirectory` / `mochaFSCloseDirectory` | 目录枚举 |
| `mochaFSStatFile` | 文件属性 |
| `mochaFSRemoveFile` | 删除（**危险**） |
| `mochaFSIoctlFile` | IO 控制（程序信息） |
| `mochaSetData` | 通用写 |

---

## 5. 轴号与坐标类型

**轴号**（C# 交付实测）：`X=1, Y=2, Z=4`（位编码，第 4 轴=8，第 5 轴=16）
> C++ 样例用枚举 `AXIS_X=1,AXIS_Y=2,AXIS_Z=3,AXIS_C=4` —— **两者不一致**，以机床实测为准（建议做成可配置）。

**坐标类型**（命令 `0x25` / `0x03` 的子码）：`1`=工件坐标 · `2`=机械坐标 · `3`=当前位置 · `6`=剩余距离

---

## 6. 读写流程

### 6.1 读机械坐标（X 轴）

```
request = bytes([
  0x47,0x49,0x4f,0x50, 0x01,0x00,0x01,0x00,
  0x44,0x00,0x00,0x00,            # 长度 68
  0x00,0x00,0x00,0x00,
  id&0xff,(id>>8)&0xff,(id>>16)&0xff,(id>>24)&0xff,   # 请求 ID
  0x01,0xff,0xff,0xff,
  0x04,0x00,0x00,0x00,
  0x01,0x00,0x00,0x00,
  0x0d,0x00,0x00,0x00,
  ord('m'),ord('o'),ord('c'),ord('h'),ord('a'),ord('G'),ord('e'),ord('t'),ord('D'),ord('a'),ord('t'),ord('a'),
  0x00,0x00,0x00,0x03,
  0x00,0x00,0x00,0x00,
  0x25,0x00,0x00,0x00,            # 命令：读坐标
  0x02,0x00,0x00,0x00,            # 子码：机械坐标
  0x01,0x00,0x00,0x00,            # 数量 1
  axis,0x00,0x00,0x00,            # 轴号
  0x00,0x00,0x00,0x00,
  0x10,0x00,0x00,0x00])           # 期望 CString
```

### 6.2 响应解析（C# 交付的 CheckFrame）

```
if len(resp) < 33:                 error(-1) 长度不足
if resp[0:8] != 47 49 4f 50 01 00 01 01:  error(-2) 报文头异常
if int32_le(resp[16:20]) != id:    error(-3) ID 不匹配
if resp[28:31] == "IDL":           无数据
else: 按 resp[28] 类型标记解析（见 §3.3）
```

---

## 7. 错误码

**设备层**（`ME_DEV_*`，响应内的状态码）
| 值 | 含义 | | 值 | 含义 |
|---|---|---|---|---|
| 0 | OK | | 21 | 数据错 |
| 1 | 已打开 | | 22 | 已取消 |
| 2 | 未打开 | | 23 | 尺寸非法 |
| 4 | 卡不存在 | | 24 | 任务退出 |
| 6 | 通道错误 | | 25 | 硬件错误 |
| 7 | 句柄错误 | | 26 | 系统停机 |
| 8 | 无法打开 | | 50 | 未知功能 |
| 10 | 未连接 | | 51 | 设定值错误 |
| 11 | 未关闭 | | 20 | **超时** |

**SDK 层**：`EZ_ERR_NOT_OPEN=0x80A00101` … `EZ_ERR_OPEN_COMM=0x80A0010C`（通用）· `EZNC_COMM_* = 0x80B00301-0305`（通信）· `EZNC_FILE_* = 0x80B00201-0210`（文件）

---

## 8. 实现坑

1. **长度字段是「总长-12」**，不是总长。写错设备直接不回。
2. **操作名长度 13 但字符串只有 12 字节**（含结尾 NUL），后面 4 字节 `00 00 00 03` 是固定填充 —— 抄样例别自己推。
3. **轴号 1/2/4 是位编码**，不是序号。
4. **`0x03` 命令的多义性**：它是通用"设备数据"命令，语义完全由子码决定（0x02 机械坐标 vs 0x7e 加工参数），实现时必须 `(cmd, sub)` 联合判断。
5. **CString 返回**：长度在 `[36]`，内容从 `[40]` 起 —— 注意偏移 4 字节的空档。
6. **单会话独占**：异常退出后必须重连，旧会话可能残留（机床侧连接数有限）。
7. **写操作风险**：`mochaSetData` 可改参数/变量；`mochaFSRemoveFile` 删程序 —— 生产环境默认禁用。

---

## 9. 参考实现

| 来源 | 说明 |
|---|---|
| EZSocket OLE 封装宏 + 样例 | 未随本目录提供 |
| `MITSUBISHI_PROTOCOL-3`（交付包） | 裸协议 C++ 实现（110 函数，报文逐字节现成） |
| C# 交付 `Form1.cs` | 19 数据点 Socket 直连实现（可直接移植） |
| EZSocket（COM） | 官方 SDK：`CLSIDFromProgID("EZSocketNc.EZNcCommunication")` → M70 用 `CLSID_...270` → `SetTCPIPProtocol(ip, port)` → `Open2(EZNC_SYS_MELDAS700M, 1, 100)`（**需授权**） |
| 完整 API 表 | 39 接口 / 271 方法 / CLSID 版本表 / 错误码全集（原始素材未随本目录提供） |
