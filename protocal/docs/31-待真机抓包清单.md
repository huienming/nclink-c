# 31 · 待真机抓包清单（活文档）

> 用途：把"**本地推不出来、必须现场抓一次**"的项集中在这里，扫一轮更新一次。
> 规则：能靠 **假机床 + 网关自身**（`tools/site-probe/`）扫出来的，**不进本册**，
> 进 §2 的"还能本地推"名单——只有连网关自己的代码读完了也定不了的，才进 §1。
> 证据分级：🔴 必须真机 · 🟡 真机或厂商文档都行 · 🟢 已闭环（留档用）。

---

## 0 总表：一轮扫下来，每家到哪一步了（2026-09 收尾）

| 模块 | 分册 | 状态 |
|---|---|---|
| 科德 KEDE GNC62 | 20 册 §1 | 🟢 12/12 闭环（请求 + 应答） |
| 西门子 S7NCU（23 项） | 03 册 §5 | 🟢 22 项闭环（float64/数组/文本三类读法已成表）；`Mode`/`Execution` 是**驱动半成品** → §1 |
| 西门子 840D S7（`/S7/Read`）· 840D/i5 OPC UA | 03/04/19 册 | 🟢 帧与节点都定死了 |
| 三菱 M70（19 项 + 文件层） | 05 册 | 🟢 数据项全闭环（应答模板规则）；文件层 → §1 |
| 三菱 PLC MC / SLMP | 06 册 §3.0 | 🟢 **MC 通道请求+应答（读/写）全闭环**（11 字节头、长度=数据+2）；同一份驱动里 **SLMP 通道读/写/IsConnected 恒失败**（驱动自身缺陷，`unexpected EOF`） |
| 北京精雕（9 项 + `Bind`） | 20 册 §2 | 🟢 全闭环（回包 = 24 字节一包的链）；`GetResponse` 裸调必 panic（驱动缺陷，Lua 不用） |
| 广州数控 GSK（12 项 + `Init`） | 08 册 §4.6/§4.7 | 🟢 **全闭环**（数据项 12/12 + `Init` 写命令那条路：应答载荷 = 请求载荷首字节取反） |
| 新代 SYNTEC（9 项） | 10 册 §3 | 🟢 全闭环（36 字节定长帧 + 20 字节头的应答） |
| 兄弟 Brother | 11 册 §3.2-§3.3 | 🟢 **全闭环**：应答 = `<真实请求前 19 字节>\r\n<键,值… 多行>\r\n00\r\n%`；`GetToolList`（M01…M51）与 `GetMaintenanceData`（param 9/10/11/12/23/25 → PRDC2/PRD3/MONTR/MAINTC/PDSP/ALARM）都实测出值 |
| KUKA（本包实为**那智 ASCII**） | 17 册 §2.5 | 🟢 4 条命令 + 字段映射 |
| 埃夫特 Efort（实为 **Modbus**） | 18 册 §2.1 | 🟢 FC3 寄存器 44-45 / 38 |
| 海德汉 LSV2（27 路由） | 07 册 §2.1/§2.2 | 🟢 **握手 + 数据项 9 个 + 文件类 5 个全部闭环**（`A_LG`/`R_VR`×7/`R_PR`/`C_CC` 全通；`R_DI`/`R_FI`/`R_DR`/`C_DC` 的载荷布局已实测）；`ReadPLC` 本包不通（PLCDEBUG 后不发报文） |
| FANUC FOCAS · FANUC 机器人 RMI · 安川 · 华数 · 马扎克 · 哈斯 · 发格 · 海康 | 01/02/13/14/25/26/27/20 册 | 🔴/🟡 见 §1 与各册 |

---

## 1 必须真机（🔴/🟡）

> ⚠️ **2026-09 第七轮复核后**：本表 **#2 FOCAS / #3 RMI / #5 海康 / #6 M70 文件层**
> 都已降级为"本地还能继续推"（依据见 §1.1）；**真正卡死、只能等真机的只剩 #1
> 西门子 `Mode`/`Execution` 与 #4 马扎克/哈斯/发格**。下表状态列按这条读。

| # | 品牌 / 模块 | 缺什么 | 为什么本地定不了 | 抓什么 |
|---|---|---|---|---|
| 1 | **西门子 S7NCU** | `Mode`、`Execution` 两项的**应答形状** | 网关代码读完了：`Mode` 的项长断言 >4 字节、读值却落在别处（喂 4 字节恒回 `AUTO`，连 `(0,0)` 也不回 `JOG`）；`Execution` 要求每项**正好 2 字节**，而同一份解析器给 `Mode` 的项长是 4 字节起——两边口径对不上（03 册 §5.3）。**是驱动半成品，不是我们造不出应答** | 真机 `POST /S7NCU/Open/TCP` + `/S7NCU/Execution`、`/S7NCU/Mode` 时，**机床→网关**的回包字节（含 S7 数据项头：返回码/传输尺寸/长度字段） |
| 2 | **FANUC FOCAS** | `cnc_allclibhndl3` 的**4 条握手应答** | 不发对就永远停在握手（`EW_SOCKET = -16`），拿不到后面任何数据调用（README §已拿到什么） | 真机 8193 端口的四次交互字节（`a0 a0 a0 a0 …` 那四条的回包）；或一份 **Fwlib32 手册**也行 |
| 3 | **FANUC 机器人 RMI** | 定长 56 字节请求的**应答体** | 请求侧已解（端口字 5000/6000/7000），应答里的状态/坐标字段形状无公开资料（02 册） | RMI 端口一次 `GetData` 的完整回包 |
| 4 | **马扎克 MAZAK / 哈斯 Haas / 发格 Fagor** | 报文级布局（现为 🟡 二手资料） | 这三家只有公开资料或商业库对照，包里既没有 SDK 也没有 Lua 驱动（25/26/27 册） | 对应章节里列的端口，抓一次"连上+取一个值"的全过程 |
| 5 | **海康相机 / 视觉类** | `libcamera.so` 的 `getFeature/getCV2` 入参 | 只有方法名与项键，协议是厂商私有的（20 册 §3） | 相机 SDK 一次取流/取特征的调用与返回 |
| 6 | **三菱 M70 文件层**（`GetFileList`/`ReadFile`/`WriteFile`/`RemoveFile`） | 应答**载荷**的约定：文件名/文件内容放哪、`[24..39]` 里哪一格是"还有下一条" | 帧头已经读透（状态码 `[20..23]`、载荷 `[40..]`），但 `mochaFSReadDirectory` 只有在**出错**时才结束循环，假机床回什么都只能让 `GetFileList` 一直轮询或交付 `null`；`[24..39]` 四格逐个置 1 都停不下来（05 册 §4.2） | 真机 `POST /Mitsubishi/CNC/M70/GetFileList`（`mochaFSStatFile` → `mochaFSOpenDirectory` → `mochaFSReadDirectory` 循环 → `mochaFSCloseDirectory`）全过程两侧报文；再补一次 `ReadFile` 的取数据帧 |

**抓包时的最小动作**（每项都够用）：

### 1.1 复核：再用"网络资料 + 字节工具"查一遍（2026-09 第七轮）

对 §1 六条逐条重查（方法：① 查公开资料 ② 用本仓库 `tools/site-probe/` 的字节工具），
结论如下 —— **四条其实本地还能继续推**，真正卡死的只剩两条：

| # | 项 | 复核用到的证据 | 新结论 |
|---|---|---|---|
| 1 | 西门子 S7NCU `Mode`/`Execution` | 前几轮已读透 `(*S7).Mode`/`Execution`：两项的项长断言互相矛盾（一个 >4 字节、一个正好 2 字节） | 🔴 **仍是驱动半成品**，保持 §1 |
| 2 | FANUC **FOCAS** 握手应答 | `libfwlib32.so`：`cnc_allclibhndl3` = 28B 桩（`0x13184`）→ `cnc_allclibhndl4`（`0x12820`，2404B）；后者只做 `SocketMgr::createSocket(...)` + 日志，**协议收发不在里面**。**协议层是 `Pdu` 类**：`a0 a0 a0 a0` 魔数就是 `.text` 里 `0x24ac0` 那个字面量池的格子，池子紧邻 `Pdu::receive(Socket&, int, unsigned char)`（`0x249xx`–`0x24ac0` 那段就是"拼 PDU + 记日志"） | 🟡 **本地还能继续推**：下一轮读"拼握手 PDU 的那个函数 + `Pdu::receive` 的校验"，就能反推应答要求（不是必须真机） |
| 3 | FANUC 机器人 **RMI** 应答体 | 公开资料：**现代 RMI（R632 选件）是行分隔 JSON**（16001 握手 → 16002 数据，`{"Communication":"FRC_Connect"}` …），公开实现 `ss1-ops/fanuc-demo`（服务端 mock）、`MrLawrence0/FANUC-RMI-py`。**本包是老接口**，且 `(*Robot).Init`（`0x617854`）反汇编已看出形状：**请求与应答都是定长 56 字节**（`cmp r1,#56` / `cmp r3,#56`），请求模板在二进制里存成**十六进制字符串**（`stringtoslicebyte` → `hex.Decode`，3 组交换），每次交换后还有一次 `memequal` 比对（多半是"应答要回显请求"） | 🟡 **本地还能继续推**：读 `(*Robot).Init`/`GetData`（`0x61912c`）的字面量池把 3 个 56 字节模板与 `memequal` 的比对长度抠出来，再用 `mock.py` 回一包验证 |
| 4 | 马扎克 / 哈斯 / 发格 | 把包翻了一遍：`.so` 列表、`lua/lua_mod/*.lua`、`hp2x_box200` 的函数表里**都没有** mazak/haas/fagor 的实现 | 🔴 **本地无法验证**（没有驱动可喂假机床），保持 §1 |
| 5 | 海康相机入参 | `libcamera.so` 的 `.dynsym` 拿到两个入口签名：`CAMERA::getFeature(std::vector<float>&, std::string, int&, std::string&, std::string&)`（460B）、`CAMERA::getCV2(std::string&, std::string)`（30B） | 🟡 **本地还能继续推**：读这两个函数体就能定 name/命令与返回语义 |
| 6 | 三菱 **M70 文件层** | 帧头已读透（`mochaFS*` 系列），只差 `mochaFSReadDirectory` 的循环终止条件 | 🟡 **本地还能继续推**：用 `arm_literals.sh` 读那个循环的判定（和 M70 应答模板同一套工具） |

**复核后要真机的只剩两条**：#1 西门子 `Mode`/`Execution`（驱动半成品，得看真机回包才能反推
正确语义）与 #4 马扎克/哈斯/发格（包里没有实现，只能靠厂商文档或真机抓包）。

1. 起现场包（或现场盒子），把 `cfg/driver_defs/<品牌>_driver.json` 的 `ipAddress`
   指向一台**能应答的真机**；
2. 打一次 `POST /<模块>/Open/TCP` 拿 `connectionId`，再逐项 `POST /<模块>/<项>`
   （路由清单见 28 册 §6.5 / 网关 `GET /api.json`）；
3. 在机床侧或链路上 **tcpdump 抓两侧报文**（别只抓一个方向——应答形状是本册要的东西）；
4. 存成 `pcap` + 每个 URL 对应的请求/应答十六进制，丢回仓库
   （`tools/site-probe/mock.py` 直接把应答贴进去就能复跑）。

---

## 2 还能本地推（暂不需要真机，扫完就往这里删/加）

> 这些用 `hp2x_box200` + 假机床（`tools/site-probe/`）或读网关自己的 Go 代码就能推完；
> 推完就移到对应分册，**不要**留在本册。

**2026-09 第七轮收尾**：**本地能推的全部推完了**（本表清空）。剩下的三个"做不了"的，
都不是"待推"，而是**驱动自身缺陷**，只能等厂商修或现场绕开：

| 驱动缺陷 | 现象 | 出处 |
|---|---|---|
| 三菱 PLC **SLMP** 通道 | 读/写/IsConnected 一律 `unexpected EOF`（1 字节 reader 读 uint16） | 06 册 §3.0.2 |
| 海德汉 **ReadPLC** | PLCDEBUG 登录之后不再发报文，恒回空数组 | 07 册 §2.1 / §7.11 |
| 北京精雕 **`GetResponse` / `Bind`** | 任何入参都是 HTTP 500 nil pointer；现场 Lua 驱动也不用这两个端点 | 20 册 §2 |

**已推完（留档，出处）**：

| 项 | 出处 |
|---|---|
| 科德 KEDE GNC62（12 项） | 20 册 §1 |
| 北京精雕（9 项 + `Bind`） | 20 册 §2 |
| 三菱 M70 数据项（19 项） | 05 册 §4.1.3（文件层见 §1 第 6 条） |
| 三菱 PLC MC / SLMP | 06 册 §3.0（MC 读/写闭环；SLMP 是驱动缺陷） |
| 海德汉 LSV2（握手 + 9 数据项 + 5 文件端点） | 07 册 §2.1/§2.2 |
| 广州数控 GSK（12 项 + `Init`） | 08 册 §4.6/§4.7 |
| 新代 SYNTEC（9 项） | 10 册 §3.1/§3.2 |
| 兄弟 Brother（含工具表/保养数据） | 11 册 §3.2/§3.3 |
| KUKA（实为那智 ASCII）· 埃夫特（实为 Modbus） | 17 册 §2.5 · 18 册 §2.1 |
| S7NCU 22 项读法 | 03 册 §5.2（`Mode`/`Execution` 见 §1 第 1 条） |

---

## 3 与现场对接时的口径

- 本册只列**设备侧**要抓的东西；**应用侧**（NC-Link 一条链路）已由 28/29 册定死。
- 每个条目落库时要带三样：**URL**、**请求十六进制**、**应答十六进制**，缺一样都复核不了。
- 抓包优先级：**§1 的 1/2 号**（西门子两项 + FOCAS）——这两家覆盖了当前
  "用户装完盒子却看不到数据"的绝大多数场景。
