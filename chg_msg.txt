01 册：上行取程序的结论落定（请求帧核完，机床不答 0x18）

* 上行三件套的请求侧已经全部核过并写进注释与 §11.15.2：0x15 start（体与下行同形那
  516 字节）、0x18 dir 4 的 8 字节数据请求、upend 收尾；file_name 三种写法、
  *length >= 256 且为 256 的倍数、读回文本以 % 结尾（再读 EW_RESET）。
* 这台模拟器上取不到文本：start 回 256 字节，但 0x18 机床一声不响（SDK 回
  EW_DATA=10、cnc_getdtailerr 细码 0）。上行的三代（upstart/upstart3/upstart4）、
  三种 file_name 写法、把 O3001 选成主程序之后 —— 全部一样。
* 所以"回读程序文本"继续如实回 NCL_ERR_UNAVAILABLE，理由改成准确的
  "0x18 那条机床不答应答"，不再写"帧待抓包"。

验证：ctest 43/43。
